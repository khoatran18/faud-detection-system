minikube start --driver=docker --cpus=10 --memory=16384
minikube mount /home/tmkhoa1812/CodingSpace/Projects/HUST/IT3930/project_2/src/train_data:/data/train_data

kubectl port-forward svc/web-ui 8080:8000 -n fpp

kubectl rollout restart statefulset minio -n fpp

killall kubectl

minikube service minio-external -n fpp


# Forward chuẩn cổng 8000 ra local để Frontend gọi được API và WebSocket
kubectl port-forward svc/web-ui 8000:8000 -n fpp &

# Giữ nguyên cổng ClickHouse để Backend xử lý dữ liệu
kubectl port-forward svc/clickhouse-internal 8123:8123 -n fpp &

# (Tùy chọn) Nếu bạn vẫn muốn xem Tabix thì bật cổng 8081
kubectl port-forward svc/tabix-external 8081:80 -n fpp &


# Nếu web 8080:8000 là expose 8000 ra 8080 local, nhưng FE mặc định gọi 8000 (local 8000 ko chạy) --> lỗi

cat /home/tmkhoa1812/airflow/simple_auth_manager_passwords.json.generated
{"admin": "kb3NRwKs7hUpA8Nx"}
